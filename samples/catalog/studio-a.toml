# Model catalog fragment owned by "studio-a" -- a second, independent
# adopter's fragment, demonstrating a plain VRAM-tiered purpose (no
# vendor/OS constraints needed for this one) merging alongside
# trusted-autonomy.toml's purpose with zero coordination between the two
# owners. This is the "multiple sub-projects each contribute a fragment,
# no central authority" scenario the catalog mechanism exists for.

[purposes.text-structured-json]
owner = "studio-a"

[[purposes.text-structured-json.tiers]]
min_vram_gb = 24
model = "qwen3:32b"

[[purposes.text-structured-json.tiers]]
min_vram_gb = 8
model = "qwen3:8b"
notes = "recommended baseline"
